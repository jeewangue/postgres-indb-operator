---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: pgdatabases.postgres.jeewangue.com
spec:
  group: postgres.jeewangue.com
  names:
    kind: PgDatabase
    listKind: PgDatabaseList
    plural: pgdatabases
    singular: pgdatabase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.phaseUpdated
      name: PhaseUpdated
      type: string
    - jsonPath: .status.error
      name: Error
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PgDatabase is the Schema for the pgdatabases API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PgDatabaseSpec defines the desired state of PgDatabase
            properties:
              host:
                type: string
              name:
                type: string
            type: object
          status:
            description: PgDatabaseStatus defines the observed state of PgDatabase
            properties:
              error:
                type: string
              phase:
                description: Phase represents the current phase of the object.
                type: string
              phaseUpdated:
                format: date-time
                type: string
            required:
            - phase
            - phaseUpdated
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
